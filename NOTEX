Manually Correct index.html
-- > replace './images/'      with '/images/'
-- > replace 'in scene gezet' with 'in scène gezet'
-- > replace 'Hèze'           with 'Hèze'
-- > replace 'jusqu'a'        with 'jusqu'à'
-- > replace 'songs.bib'      with 'songs' 